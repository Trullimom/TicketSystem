@{
    Layout = "_EingeloggtAdmin";
}

@model Anfrage


<form asp-controller="Admin" asp-action="AddTicket" method="post" class="contact-form">
    <div class="form-group">
        <label><strong>Projekt wählen:</strong></label>
        <select asp-for="@Model.ProjektName" class="form-select">
            <option value=" ">Wählen</option>
            @foreach (var projectName in AnfragenListe.GetProjekteNamen())
            {
                <option value="@projectName">@projectName</option>
            }
        </select>
        <label><strong>Neues Projekt:</strong></label>
        <input type="text" asp-for="@Model.NeuesProjekt" placeholder="Wenn Projekt nicht schon vorhanden ist" class="form-control bg-light">
        <label><strong>Firmen Name:</strong></label>
        <input type="text" asp-for="@Model.KundenName" placeholder="Firmen Name" class="form-control bg-light">
    </div>

    <div class="form-group">
        <label><strong>Ansprechpartner: </strong></label>
        <input type="text" asp-for="@Model.Ansprechpartner" placeholder="Name" class="form-control bg-light">
    </div>

    <div class="form-group">
        <label><strong>Email: </strong></label>
        <input type="text" asp-for="@Model.Email" placeholder="Email" class="form-control bg-light">
    </div>

    <div class="form-group">
        <label><strong>Telefonnummer: </strong></label>
        <input type="text" asp-for="@Model.Telefon" placeholder="Telefon" class="form-control bg-light">
    </div>
 
    <div class="form-group">
        <label><strong>Wunschtermin: </strong></label>
        <input type="date" asp-for="@Model.DeadLine" value="" min="1997-01-01" max="2030-12-31" placeholder="Wunschtermin" class="form-control bg-light">
    </div>

    <div class="form-group">
        <label><strong>Ihre Nachricht an uns: </strong></label>
        <textarea asp-for="@Model.Nachricht" placeholder="Nachricht" class="form-control bg-light" rows="4" style="resize: vertical;"></textarea>
    </div>
    <input asp-for="@Model.Datum" type="hidden"/>

    <button type="submit" class="btn btn-primary mt-3">Senden</button>
    <button class="btn btn-primary mt-3"><a class="text-light" asp-action="AnfragenTabelleAdmin">Zurück zur Tabelle</a></button>
</form>